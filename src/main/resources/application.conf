ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }

    application {
        modules = [ no.nav.tiltakspenger.dokument.ApplicationKt.module ]
    }
}

endpoints {
    pdf = ${?PDF_ENDPOINT_URL}
}